_target_: pytorch_lightning.loggers.wandb.WandbLogger
project: ${project}
group: ${group}
tags: ${tags}
name: ${project},${group},${comments}
save_dir: ${hydra:runtime.output_dir}
log_model: True 
offline: False
id: null 
anonymous: null 
prefix: "" 
job_type: ""

